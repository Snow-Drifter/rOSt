{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch with BIOS",
            "request": "launch",
            "type": "node-terminal",
            "command": "cargo run bios",
            "env": {
                "RUSTFLAGS": "-C no-redzone"
            }
        }
    ]
}